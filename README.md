# Shortest Possible String

This program generates the shortest possible string (called an incantation) by concatenating N input strings (each of length L) while maximizing overlap between adjacent strings. The goal is to avoid unnecessary duplication when combining the sequences.
